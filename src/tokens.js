/**
 * Separate expression into array tokens
 * 
 * @param {String} expression
 * @return {Array} of String
 */
Expression.prototype._tokens = function (expression) {
    var tokens = [];

    return tokens;
};
